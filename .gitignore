node_modules/
.env
.env.local

# receptionist-server's local booking store - runtime state, not source.
# On most hosts (Render free tier, etc.) this resets on every deploy/restart
# anyway since the filesystem isn't persistent - see receptionist-server's
# deployment notes.
bookings.json

# Claude Code local settings (per-machine, not shared)
.claude/settings.local.json

# Generated per-company output from receptionist-server/generate-assistant.js
# and scripts/generate-marketing-site.js - real business content, not a
# source template, and different for every client deployment.
receptionist-server/assistant.generated.json
marketing-site.generated.html

# Vite build output
dist/
